import { Box, Typography } from '@mui/material';

export default function Banner() {
  return (
    <Box>
      <Typography component="h1" sx={{ color: '#ffffff', fontSize: '30px' }}>
        Banner
      </Typography>
    </Box>
  );
}
